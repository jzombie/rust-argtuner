# Plan: Fix ambiguous READMEs (root + all sub-READMEs + create missing crate READMEs)

## Task
Address the code-review findings:
- **SEV-2 / Unintelligible Product Definition**: the root README never defines the product. The one-line intro `Project-based command-template tuner CLI.` relies on prior knowledge; "tuner" is overloaded (radio/guitar/ML) and ambiguous.
- **Failure to Orient**: the root README plunges into `## When it fits` without establishing What/How/Why.
- The same orientation gap exists in all five sub-READMEs, which use "tuner", "ARGTUNER", and "talkback" without ever defining argtuner — and the guitar demo compounds the overloaded "tuning" term in the musical sense.
- Three crates have **no README at all** (`crates/common`, `bindings/talkback-derive`, `crates/mock-bin`) — two of them published crates — so the orientation gap extends to them.

## Consistent principle applied to every file
Every README gets a **surgical orientation edit** (not a rewrite): each non-root README gains a 1–2 sentence opening that (a) defines argtuner as a black-box hyperparameter optimization CLI, and (b) states the file's exact role in that ecosystem. This mirrors the review's demand for precision without adding bloat.

---

## 1. Root `README.md`

**Problem**: Ambiguous one-line intro; no What/How/Why before `## When it fits`.

**Change**: Replace the line at README.md:3

```markdown
Project-based command-template tuner CLI.
```

with this verbatim text (per review directive):

```markdown
`argtuner` is a language-agnostic CLI tool for black-box hyperparameter optimization. It repeatedly executes a target script or binary while systematically varying its command-line arguments to find the optimal configuration.

By defining a search space in `argtuner.toml` and templating your command (e.g., `--lr {lr}`), `argtuner` orchestrates trials using algorithms like Particle Swarm Optimization (PSO) or Successive Halving. It reads trial metrics directly from the process `stdout` and logs results to a local SQLite/CSV database.
```

This establishes What (black-box hyperparameter optimizer), How (search space + command templates + PSO/SH + stdout metrics), and Why/Where (SQLite/CSV results) before `## When it fits`.

---

## 1b. Root `README.md` — registry-safe absolute URLs (AMEND)

**Problem**: `argtuner` is publishable by default (no `publish = false`). crates.io renders the README as a standalone HTML page, so relative links resolve against `crates.io/crates/...`, not against tarball contents — every relative link dead-ends.

**Change**: Convert every relative path reference to an absolute `https://github.com/jzombie/rust-argtuner/blob/main/...` URL. Exact edits (line refs are current):
- L86 `[guitar tuning demo](examples/guitar_tuning/README.md)` → `https://github.com/jzombie/rust-argtuner/blob/main/examples/guitar_tuning/README.md`
- L109 `[interactive probe](examples/interactive_probe/README.md)` → `.../examples/interactive_probe/README.md`
- L120 `[loss-pattern generator](examples/loss_pattern_generator/README.md)` → `.../examples/loss_pattern_generator/README.md`
- L66 `` `examples/linear_regression/` `` (inline code) → link `` [`examples/linear_regression/`](https://github.com/jzombie/rust-argtuner/blob/main/examples/linear_regression/README.md) ``
- L309-311 `` `crates/common/assets/protocol.schema.json` `` (inline code) → link `` [`crates/common/assets/protocol.schema.json`](https://github.com/jzombie/rust-argtuner/blob/main/crates/common/assets/protocol.schema.json) ``

**Keep unchanged**: L21 `[Watch](#watch-live-tui)` (same-document anchor — renders correctly on crates.io); `` `tests/protocol_schema.rs` `` (own-package path, exists in the argtuner tarball); logical names (`argtuner.toml`, `trials.csv`, `::ARGTUNER::`, `RESULT_PREFIX`) are identifiers, not file links.

---

## 2. `examples/guitar_tuning/README.md`

**Problem**: "tunes a virtual six-string guitar" uses "tuning" in the musical sense, compounding the root README's ambiguity. Mentions `ARGTUNER JSON events`, `argtuner.toml`, and the "PSO sampler" without ever stating that the six string frequencies ARE the hyperparameters and `mean_abs` IS the metric being minimized.

**Change**: Replace the opening sentence (line 3) with an orientation that bridges musical tuning → hyperparameter tuning:

```markdown
`argtuner` is a black-box hyperparameter optimization CLI; this demo maps that model onto guitar tuning. The six string frequencies (`{e2}`..`{e4}`) are the search-space parameters and the mean absolute frequency error is the metric `argtuner` minimizes with Particle Swarm Optimization (PSO). The target frequencies follow standard tuning (E2–A2–D3–G3–B3–E4).
```

(Keeps the existing standard-tuning detail, which now reads as the optimization target rather than the ambiguous subject.)

---

## 3. `examples/linear_regression/README.md`

**Problem**: "demonstrating the simplest possible ARGTUNER integration" never defines ARGTUNER or what "integration" means to a newcomer.

**Change**: Replace line 3's paragraph with:

```markdown
`argtuner` is a black-box hyperparameter optimization CLI that runs a command repeatedly with varying arguments. This example is the minimal integration: a gradient-descent linear regression that emits a single `model.epoch_end` metric event on stdout for `argtuner` to score.
```

---

## 4. `examples/interactive_probe/README.md`

**Problem**: "Interactive CLI that emits ARGTUNER event lines so you can manually drive the tuner" never defines argtuner, the "tuner", or the talkback protocol those event lines belong to.

**Change**: Replace lines 3–4's paragraph with:

```markdown
`argtuner` is a black-box hyperparameter optimization CLI whose talkback protocol lets a trial command report metrics on stdout. This probe is a REPL that emits those protocol events by hand (`model.epoch_end`, `model.early_stopped`, `model.invalid_config`) so you can exercise argtuner's logging and TUI without a real training script.
```

(The Command list below already enumerates the emittable events, so no further changes needed.)

---

## 5. `examples/loss_pattern_generator/README.md`

**Problem**: Already names `argtuner` but still doesn't define it — "demonstrates `argtuner`" presumes knowledge.

**Change**: Extend the first sentence (line 3) with an orientation clause:

```markdown
`argtuner` is a black-box hyperparameter optimization CLI that repeatedly runs a command with varying arguments; this project demonstrates it with a synthetic loss generator. It simulates model training processes with various characteristics (smooth decay, overfitting, spikes, etc.) to test the tuner's visualization and optimization capabilities without the overhead of training real machine learning models.
```

(No changes needed to the accurate `How it works` section.)

---

## 6. `bindings/rust/README.md`

**Problem**: "Tiny Rust bindings for emitting ARGTUNER JSON event lines" never defines what ARGTUNER is or what those event lines are FOR — a reader can't tell this is the client side of an optimizer's stdout protocol.

**Change**: Replace lines 3–5's paragraph with:

```markdown
`argtuner` is a black-box hyperparameter optimization CLI that reads trial metrics from a command's stdout. `argtuner-talkback` is the client-side crate your training script uses to emit those protocol messages (the "talkback" lines). It is the canonical reference for the protocol and is intentionally small so that other language bindings can mirror it easily.
```

(Keeps the canonical/schema guarantee sentences verbatim below.)

**Uniformity (AMEND)**: `argtuner-talkback` is also published. Its L11 inline code `` `crates/common/assets/protocol.schema.json` `` is a cross-crate path reference → convert to an absolute link: `` [`crates/common/assets/protocol.schema.json`](https://github.com/jzombie/rust-argtuner/blob/main/crates/common/assets/protocol.schema.json) ``. Crate names (`argtuner-common`, `argtuner_common::TalkbackMessage`) are identifiers, not file links — leave unchanged.

---

## 7. NEW: `crates/common/README.md` (`argtuner-common`) — create

**Justification**: Published crate (`publish.workspace = true`) and the single source of truth for the talkback protocol (`TalkbackMessage` → JSON Schema), yet it has no README. A reader landing here cannot tell what the crate is or why the schema-guarantee claim matters.

**Create** `crates/common/README.md` (~15 lines):

```markdown
# argtuner-common

Shared constants and protocol types for the argtuner workspace. `argtuner` is a
black-box hyperparameter optimization CLI; this crate defines the wire format of
the "talkback" protocol a trial command speaks on stdout (`::ARGTUNER::`-prefixed
JSON lines).

Highlights:

- `TalkbackMessage` — the single wire type; `protocol_schema()` generates the
  JSON Schema from it so the schema can never drift from the emitter.
- `RESULT_PREFIX` — the `::ARGTUNER::` line prefix the tuner parses.
- `EventKind` / event-name constants (`model.epoch_end`, `model.early_stopped`, ...).
- `render_starter_toml` / `STARTER_TEMPLATE_TOML` — starter `argtuner.toml` skeleton
  shared by all bindings.
- `STEP_PUBLISHER_PORT` — real-time step publisher for the TUI.

The canonical schema is committed at `assets/protocol.schema.json`; the full
protocol is documented in the [repository root README](https://github.com/jzombie/rust-argtuner/blob/main/README.md).
```

> **Registry-rendering rule (AMEND)**: `argtuner-common` is published standalone
> via `cargo publish`; its README must not reference sibling paths (they are
> absent from the published tarball). Hence the root-README reference is an
> **absolute HTTPS URL** to the `main` branch — verified to exist (see
> Verification). No relative links of any kind in this file.

---

## 8. NEW: `bindings/talkback-derive/README.md` (`argtuner-talkback-derive`) — create

**Justification**: Published crate (`publish.workspace = true`) whose API currently exists only as one bullet inside `bindings/rust/README.md`; no standalone doc for the crate's own page.

**Create** `bindings/talkback-derive/README.md` (~12 lines):

```markdown
# argtuner-talkback-derive

Procedural macro helpers for `argtuner-talkback` — the client-side crate a
training script uses to report metrics to `argtuner` (a black-box
hyperparameter optimization CLI) over stdout.

`#[talkback_args]` — attribute macro placed above `#[derive(Parser)]` on a clap
struct. It injects three `--arg(long, ...)` fields unless already present:

- `--print-template` — print the rendered command template and exit.
- `--print-template-toml` — print a starter `argtuner.toml` and exit.
- `--print-protocol-schema` — print the talkback protocol JSON Schema and exit.

Requires a struct with named fields. Full API docs:
[`argtuner-talkback`](https://github.com/jzombie/rust-argtuner/blob/main/bindings/rust/README.md).
```

> **Registry-rendering rule (AMEND)**: `argtuner-talkback-derive` is published
> standalone via `cargo publish`; its README must not use the relative link
> `../rust/README.md` (the sibling directory is absent from the published
> tarball). The link above is an **absolute HTTPS URL** to the `main` branch —
> verified to exist (see Verification). No relative links of any kind in this file.

---

## 9. NEW: `crates/mock-bin/README.md` (`argtuner-mock-bin`) — create (low priority)

**Justification**: Not published (`publish = false`); purely internal test fixtures. A short orientation prevents a contributor from mistaking these binaries for real tools.

**Create** `crates/mock-bin/README.md` (~8 lines):

```markdown
# argtuner-mock-bin

Test-only mock subprocesses for the argtuner test harness. Each binary acts as a
fake training script that emits `::ARGTUNER::` events over stdout, simulating a
trial command for integration tests. They are consumed by
`argtuner::test_support::bin_command` and are never published to crates.io.

Binaries: `mock_emit_result`, `mock_emit_env_result`, `mock_emit_invalid_result`,
`mock_emit_binding_version`, `mock_emit_x_used`.
```

---

## Verification
- **Fact-checked against source** (all statements in the proposed edits are true today):
  - PSO sampler → `src/sampler/pso.rs`; Successive Halving scheduler → `src/scheduler/successive_halving.rs`.
  - SQLite/CSV persistence → `src/trial/db.rs` (rusqlite), `src/trial/store.rs`, `TRIALS_CSV_FILENAME` in `src/constants.rs`.
  - Example configs confirm each proposed edit: `guitar_tuning/argtuner.toml` (sampler `pso`, metric `mean_abs`, goal `min`), `linear_regression/argtuner.toml` (sampler `random`, metric `loss`), `interactive_probe/argtuner.toml` (metric `metric`), `loss_pattern_generator/argtuner.toml` (metric `val_loss`).
  - New-crate README claims verified: `argtuner-common` exposes `TalkbackMessage`, `protocol_schema`, `RESULT_PREFIX`, `EventKind`, `render_starter_toml`, `STEP_PUBLISHER_PORT` (`crates/common/src/lib.rs`); `argtuner-talkback-derive` injects `print_template`, `print_template_toml`, `print_protocol_schema` (`bindings/talkback-derive/src/lib.rs`); `argtuner-mock-bin` is `publish = false` with the five mock binaries listed (`crates/mock-bin/Cargo.toml`, `crates/mock-bin/src/bin/`).
- **Registry-links verified (AMEND)**: remote default branch is `main` (`git ls-remote --symref origin HEAD`). All absolute URLs used across the plan were fetched and confirmed to exist on `main`: `https://raw.githubusercontent.com/jzombie/rust-argtuner/main/README.md`, `.../main/bindings/rust/README.md`, `.../main/examples/{guitar_tuning,interactive_probe,loss_pattern_generator}/README.md`, `.../main/examples/linear_regression/README.md`, and `.../main/crates/common/assets/protocol.schema.json`.
- **Uniform absolute-URL policy (AMEND)**: every README belonging to a publishable crate (`argtuner`, `argtuner-common`, `argtuner-talkback`, `argtuner-talkback-derive`) uses **absolute `blob/main/` URLs** for any cross-file/cross-crate reference. crates.io renders READMEs as standalone HTML, so relative links resolve against crates.io and 404 regardless of tarball contents. Same-document anchors (e.g. `[Watch](#watch-live-tui)`) and own-package paths are kept. Non-published docs (`examples/*/README.md`, `crates/mock-bin/README.md`) may keep plain relative paths but contain none.

## Audit result (post-implementation verification) — PASS
Re-read all 9 READMEs and re-verified every introduced claim against source on 2026-08-11:
- **Plan conformance**: every edit matches its plan section verbatim — root intro (L3–5), the five absolute-URL conversions (L68, L88, L111, L122, L312; anchor L23 kept), the five sub-README orientation paragraphs (each file's opening paragraph), the `bindings/rust` schema link (L9), and the three new crate READMEs byte-for-byte.
- **Factual accuracy re-verified against source**:
  - `guitar_tuning`: six params `{e2}`..`{e4}` in `argtuner.toml`; metric `mean_abs`, goal `min`, sampler `pso`; `TARGET_STRINGS` in `main.rs` are exactly standard tuning (82.41/110/146.83/196/246.94/329.63 = E2/A2/D3/G3/B3/E4); `checkpoint_dir` accepted and ignored (`let _ = args.checkpoint_dir`, main.rs:42); `fixed` scheduler `n_trials=60`, `seed=7` confirmed in toml.
  - `linear_regression`: `main.rs` is genuinely gradient descent (weight -= lr·grad, bias -= lr·grad) and emits a **single** `emit_epoch_end` (main.rs:58) with `loss`; toml confirms random sampler, 5 trials, `lr` log-scale.
  - `interactive_probe`: command list supports the three events named in the new intro (`result`→`model.epoch_end`, `event`→`model.early_stopped`, `invalid`→`model.invalid_config`).
  - `loss_pattern_generator`: toml confirms the five pattern choices, `metric_key = "val_loss"`, goal `min`.
  - `bindings/rust`: every API name in the Core API list exists in `bindings/rust/src/lib.rs` (`emit_event`, `args_map`, `parse_args`, `maybe_print_protocol_schema_and_exit`, `print_protocol_schema`, `maybe_print_template_and_exit`, `init_with_args`, `render_template_command`, `render_template_toml`, `Talkback::init`).
  - `crates/common`: `protocol_schema()` builds the schema from `TalkbackMessage` via `schemars` (`protocol.rs:100-101`); all other highlights (`RESULT_PREFIX`, `EventKind`, `render_starter_toml`, `STARTER_TEMPLATE_TOML`, `STEP_PUBLISHER_PORT`) present in `lib.rs`.
  - `talkback-derive`: `lib.rs` injects the three fields **unless already present** and errors on non-named-field structs.
  - `mock-bin`: five binaries confirmed on disk; consumed via `argtuner::test_support::bin_command` (`src/test_support.rs:61`); `publish = false` in `Cargo.toml`.
- **Incidental pre-existing finding (outside plan scope)**: root README L16 says "ships four subcommands" but lists five (`run`, `rebuild-csv`, `watch`, `find`, `plan`). Pre-dates this work; not introduced by the plan. Recommend a one-word fix (`four` → `five`) if the reviewer returns.

## Steps
1. Apply the root README intro edit (exact verbatim text).
2. Apply the root README absolute-URL conversions (section 1b).
3. Apply the five sub-README orientation edits; convert the `bindings/rust` schema path reference per uniformity.
4. Create the three new crate READMEs (`crates/common/`, `bindings/talkback-derive/`, `crates/mock-bin/`), using the absolute HTTPS URLs shown above for cross-crate references in the two published crates.
5. `git diff` to confirm only the intended lines changed and no factual drift.
6. `cargo build` is not required (docs-only changes); optional sanity check.
