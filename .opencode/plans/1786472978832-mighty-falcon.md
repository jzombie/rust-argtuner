# Plan: README housekeeping — move top TODOs to TODO.md + how-to-first section reorder

## Goal

Per review + user decisions:
1. Move the 3 TODO lines at the top of `README.md` into a new `TODO.md` at the repo root.
2. Reorder `README.md` so how-to/config content comes before the wire-protocol deep-dive, and extract that deep-dive (event protocol + JSON schema + worked parsing example) into a dedicated `## Protocol` section near the bottom (progressive disclosure).

## Target section order

```
# argtuner  (title + one-line intro)
## When it fits
## CLI subcommands                     (moved up from line ~184)
## Command templates                    (usage only: placeholders, reserved tokens, execution flow; deep-dive removed)
## Example: linear regression
## Example: guitar tuning demo
## Example: interactive probe
## Example: loss-pattern generator
## Config layout (`argtuner.toml`)      (moved up from line ~437)
## Optimization                        (moved up from line ~307)
## Schedulers (budgeted runs)
## Watch (live TUI)                    (moved after Schedulers, from line ~203)
## Protocol                             (NEW: "Result fields / event protocol" + schema echo + worked example)
## Command flow (tuner ↔ app)
## Hyperparameter impact report
## CSV behavior
```

All heading texts stay identical, so the internal anchor `[Watch](#watch-live-tui)` (README ~line 193) keeps resolving.

## Changes

### 1. `TODO.md` (new, repo root)
Three bullet items, taken verbatim from README lines 1, 2, and 4 (prior-state comparison / stop-on-exhaustion; Burn example as integration test; stateless-environment README note).

### 2. `README.md`
Mechanical block moves. **Every section's text is preserved verbatim** — in particular the `<!-- protocol.schema.json -->` `<details>` block and the `<!-- protocol.example.feed -->` / `<!-- protocol.example.parsed -->` worked-example blocks must stay byte-for-byte: the tests `readme_echoes_current_schema` and `readme_example_extracts_payload` locate them by marker (position-independent), so moves are safe as long as content is untouched.

- Delete lines 1–4 (the three TODOs + interstitial blank).
- Split `## Command templates` (currently lines 17–183): drop everything from the `Result fields / event protocol:` label through the end of the worked-example bullets (~48–182). What remains is pure template usage.
- Create `## Protocol` with that extracted content, inserted just before `## Command flow`. Promote the leading bold label to a `### Result fields / event protocol` sub-heading.
- Move the section blocks into the target order above (CLI subcommands up; four Examples up; Config layout up; Optimization up; Watch after Schedulers). Exact line ranges re-derived from the file at execution time.

## Files modified

- `README.md`
- `TODO.md` (new)

## Verification

- `cargo test -p argtuner --test protocol_schema` — all 5 tests pass (schema echo + worked-example assertions are marker-based and content-preserving).
- `grep -n "TODO" README.md` → no matches at the top (README clean; items only in TODO.md).
- `grep -n "^## " README.md` → the 16 headings appear in exactly the target order above.
- Visual check: schema `<details>` still renders; internal `[Watch](#watch-live-tui)` link intact.
