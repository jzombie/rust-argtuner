# Plan: Main README — standalone/inference use case + `#[param]` parameter reference

## Goal
Address the two gaps in the main `README.md`:
1. Show that the same binary runs **directly** (single-shot training/eval/inference) without argtuner.
2. Document **how to specify additional parameters** (the `#[param(...)` surface + supported types).

## Changes (all in `README.md`)

### 1. Quickstart — promote the standalone use case
Replace the one-line footnote at the end of step 3 (`Run the binary directly
anytime for a normal CLI — emission silently no-ops…`) with a proper block:

```markdown
**Use it directly, too.** The same binary is a normal CLI — run it for
single-shot training, evaluation, or inference without argtuner:

```bash
./my_model --lr 0.002 --steps 200
```

`--help`, defaults, and validation all work; emission silently no-ops when not
running under argtuner, so `stdout` stays clean.
```

### 2. New `## Declaring parameters` section, right after `## Quickstart`
Covers how to add arbitrary parameters:

```markdown
## Declaring parameters

Each field of your `#[talkback_args]` struct becomes a `--flag <name>` CLI
argument and a template placeholder; its doc comment becomes the `--help` text.
`#[param(...)]` hints control the rest:

- `default = 0.001` — CLI default; fields without one are required (unless `Option<T>`).
- `min = …` / `max = …` — search-space bounds → `Float`/`Int` `[space]` entry.
- `log = true` — log-scale range (floats).
- `step = …` — stepped (linear) range.
- `choices = ["a", "b"]` — categorical → `Choice` `[space]` entry + CLI validation.
- `value_name = "trial_dir"` — reserved placeholder, injected by argtuner.
- `long = "checkpoint-dir"` — override the `--flag` name.

Supported field types: `f64`/`f32`, integers (`usize`, `i64`, …), `String`,
`bool` (becomes a flag), and `Option<T>` (optional); other `FromStr` types work
too. Fields with no `min`/`max`/`choices` are fixed CLI args — baked into the
generated template as their default and excluded from `[space]`.

See the [`argtuner-talkback` README](https://github.com/jzombie/rust-argtuner/blob/main/bindings/rust/README.md)
for the full reference.
```

(Kept as a compact list, not a table, to match the README's style.)

## Verification
1. `cargo test --workspace --all-features --tests` — README assertion tests still
   pass (no marker blocks touched; `bash`/text blocks aren't doctested).
2. `cargo test --workspace --all-features --doc` — still green (no `rust` blocks added).
3. Read-through: standalone/inference is clearly shown after step 3; the
   `Declaring parameters` section lists all `#[param]` keys and types.
