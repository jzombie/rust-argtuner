# Plan: Restructure the README into a linear learning path

## Goal
Reorder `README.md` so the top is a fast onboarding flow (the config_showcase as
the single worked example) and the deeper technical docs become a reference
manual at the bottom. The five README self-assertion blocks (markers +
fenced blocks in `tests/readme_assertions.rs`) must be preserved **verbatim**
(marker immediately followed by its fenced block) so the tests keep passing.

## New section order (all in `README.md`)
Current: Installation → When it fits → CLI subcommands → Command templates →
Config layout → Optimization → Schedulers → Watch → Protocol → Command flow →
Hyperparameter impact → CSV → License.

New:
1. `# argtuner` + badges + intro (keep as-is)
2. `## When it fits` — moved up (currently lines 50–55), content unchanged
3. `## Installation` — moved up (currently lines 11–48), content unchanged
4. `## Configuration showcase` — **merge** of `Command templates` +
   `Config layout` (below)
5. `## CLI subcommands` — moved up; now lists **six** subcommands
   (add `inspect`); gains the Watch content as `### Watch (live TUI)`
6. `## Protocol` — moved up; content unchanged
7. `## Optimization` — moved down; content unchanged
8. `## Schedulers (budgeted runs)` — moved down; content unchanged
9. `## Command flow (tuner ↔ app)` — moved down; **remove the duplicate
   "execution flow" list** (kept once, in the Showcase); keep env vars,
   Windows note, error/duplicate handling
10. `## Hyperparameter impact & CSV behavior` — merge the two sections as
    `### Hyperparameter impact report` + `### CSV behavior`
11. `## License` — unchanged
12. Reference link definitions at the bottom — unchanged

## Configuration showcase section (the merge)
Combines the old "Command templates" + "Config layout" into one section:
- Lead-in: the single worked example is the bundled
  `examples/config_showcase/` project.
- Template concept: `{placeholder}` tokens, `{{`/`}}` escaping (prose from old
  Command templates).
- Full `argtuner.toml` — the existing `<!-- config.example -->` block, verbatim.
- Reserved-placeholder bullets (`trial_id`, `trial_dir`) — unchanged.
- `argtuner inspect examples/config_showcase` + the `<!-- config.inspect.output -->`
  block, verbatim.
- The "execution flow" list — kept here as the single copy.
- The existing config-layout "Notes:" bullets — unchanged.
- Drop the old cross-link to `#config-layout-argtunertoml` (now same section).

## Other edits while restructuring
- CLI subcommands: change "ships five subcommands" → six; add an `inspect`
  bullet; add `argtuner inspect examples/config_showcase` to the example block.
- Fix stale prose in `## Protocol`: the two references to
  `tests/protocol_schema.rs` asserting the schema/feed now live in
  `tests/readme_assertions.rs` (the schema-echo and feed tests moved there
  earlier).
- Keep the `[Watch](#watch-live-tui)` link valid (Watch becomes a `###` heading
  with the same slug).

## Verification
1. `cargo test --all-features --test readme_assertions` — all 5 marker-based
   tests must still pass (proves the moved blocks are byte-identical).
2. `cargo test --all-features --test protocol_schema` — unaffected, still green.
3. Read-through: confirm section order, that no internal anchor is broken, and
   that the Onboarding (intro → when-it-fits → install → showcase → cli) flows
   before the Reference (protocol → optimization → schedulers → command flow →
   impact/csv → license).
4. Optionally `cargo clippy --all-features --all-targets` (no code changes
   expected; README-only).
