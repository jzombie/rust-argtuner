# Plan: Remove the `guitar_tuning` example

## Rationale
`examples/guitar_tuning/` is dead weight: nothing references it (no test, no
workflow, no README section), its PSO/multi-float/talkback pedagogical points
are covered by `loss_pattern_generator` + `config_showcase`, and it commits
stale `artifacts/`, `trials.csv`, and `trials.sqlite`. All other examples stay:
`interactive_probe` (dev REPL), `linear_regression` (end-to-end test fixture in
`tests/linear_regression.rs`), `loss_pattern_generator` (drives `config_showcase`
and the TUI), `config_showcase` (README-featured).

## Changes
1. Delete the directory: `git rm -r examples/guitar_tuning`
   (main.rs, argtuner.toml, README.md, artifacts/, trials.csv, trials.sqlite).
2. `examples/config_showcase/README.md:5` — drop the passing mention:
   "Unlike the runnable examples (`linear_regression`, `guitar_tuning`, ...), this"
   → "Unlike the runnable examples (`linear_regression`, `loss_pattern_generator`, ...), this"

No other references exist (verified by grep). Examples are Cargo auto-discovered,
so no Cargo.toml change is needed.

## Verification
1. `cargo test --workspace --all-features --lib --bins --tests --examples` —
   full suite (incl. CI's `--examples` flag) still green.
2. `cargo build --all-features --examples` — the remaining four examples compile.
3. Confirm `git status` shows only the deletion + the config_showcase README edit.
