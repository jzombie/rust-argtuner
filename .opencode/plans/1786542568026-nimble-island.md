# Plan: Implement Epics B, A, C — process hygiene, conditional params, multi-objective

Executed in order **B → A → C**, each a complete verifiable phase. (The earlier
Items 1–3 — README Bool docs, Bool tests, lightweight `--no-default-features` —
are already implemented and remain.)

---

## Phase 1 — Epic B: Process-tree hygiene & per-trial timeouts

**Goal:** no orphaned sub-child processes on trial abort/timeout; config-driven
per-trial timeout.

### B1. Process groups
- Add `command-group` as a workspace + argtuner dep for the **piped** path
  (`command-group` extends `std::process::Command`/`Child`).
- The **PTY** path uses `portable_pty::CommandBuilder` (returns a
  `portable_pty::Child` trait object, **not** `std::process::Child`, so
  `command-group` does not apply): spawn with
  `CommandBuilder::process_group(true)` (Unix), then kill the group natively —
  `process_id()` returns `Option<u32>`, so unwrap before the signal:
  ```rust
  if let Some(pid) = child.process_id() {
      // reaped-guard checked before this
      unsafe { libc::kill(-(pid as i32), libc::SIGKILL); }
  }
  ```
  Windows never uses the PTY path (the runner is piped there), so Job Objects via
  `command-group` cover Windows; if a Windows PTY path is ever added, attach the
  PID to a Job Object before spawn.
- `src/command/subprocess/runner.rs`:
  - Piped path (`run_piped`): `command_group::GroupCommand` → `GroupChild`, kill
    the group as a unit.
  - PTY path (non-Windows): keep the pgid (child pid) and `libc::kill(-pgid,
    SIGKILL)` on teardown.
  - Runner accepts `timeout: Option<Duration>` + a kill/cancel signal.

### B2. Per-trial timeout + cancel
- Config: add `trial_timeout_s: Option<u64>` to `[scheduler]` (`SchedulerConfig`),
  0/absent = none.
- Runner: when a timeout is set, a watchdog kills the process group after the
  deadline; the trial is marked `error` with a "timed out" reason.
- Cancel: the existing `stop_flag` (Ctrl-C / TUI quit) now also kills the active
  trial's process group, not just waits.
- **Group-kill is synchronized against the reap:** a shared `AtomicBool`/
  `Arc<Mutex<Option<Child>>>` marks the child reaped as soon as the wait loop
  collects it; watchdog/cancel paths check it **before** calling
  `kill(-pgid, SIGKILL)` and skip if already reaped — preventing a recycled PGID
  from being killed.
- `src/command/objective.rs` wires timeout + group-kill into trial execution;
  `src/tuner/mod.rs` passes the timeout through.

### B3. Tests + verification
- Unit: piped spawn starts a grandchild (e.g. `sh -c "sleep 100 & sleep 100"`);
  killing the group reaps both children (verify no survivors via `ps`).
- Timeout: a mock that sleeps beyond the timeout → trial errors with "timed out",
  child reaped.
- `cargo test --workspace --all-features`; clippy; CI cross-platform (Unix +
  Windows) since process-group semantics are OS-specific.

---

## Phase 2 — Epic A: Conditional hyperparameters

**Goal:** a param is active only when its parent sampled an allowed value; its
argument is **omitted** from the rendered command when inactive.

### A1. Search-space AST (`src/search_space/mod.rs`, `src/sdk.rs`, derive)
- `ParamSpec` (and `ParamHint`) gain:
  ```rust
  parent: Option<String>,
  parent_values: Option<Vec<String>>,
  ```
  Serialized as `parent = "optimizer"`, `parent_values = ["sgdt"]` in
  `[[space.params]]`; `ParamSpecPre`/`TaggedParamSpec` + `From` updated; the
  derive parses `#[param(parent = "optimizer", parent_values = ["sgdt"])]`.
- DAG validation in `validate_specs`:
  - parent must exist; no cycles; parent must yield a finite set of string
    values — `Choice`, **`Bool`** (`parent_values` = `["true"]`/`["false"]`), or
    a fixed string-valued param.
  - `parent` and `parent_values` are paired: both set or both `None`;
    `parent_values` must be non-empty.
  - `parent_values` domain containment: every value must be a permitted value of
    the parent (`parent.choices` for `Choice`, or `["true", "false"]` for
    `Bool`); otherwise the child is permanently unreachable and the config is
    rejected.
  - conditional params (with a `parent`) must be bound to a named `--flag`
    (positional placeholders are rejected — there's no flag token to strip).

### A2. Conditional sampling + rendering
- `SearchSpace::values_from_unit`/`fields_from_unit` evaluate in dependency
  order and **omit** inactive params (no value, no `hp.*` field).
- `src/command/template.rs` / `src/trial/mod.rs` rendering: after filling
  placeholders, strip each inactive param's flag segment from the command. The
  token-aware removal matches both `--flag {value}` (two shell tokens) and
  `--flag={value}` (one unified token), respecting shell quoting. Conditional
  params are guaranteed flag-bound by `validate_specs` (positionals rejected), so
  there is always a flag token to strip. Evaluate conditions against the sampled
  parent value.
- Random sampler's `DiscreteConfigPool` enumerates only parent-valid
  combinations (duplicate detection stays correct).

### A3. Inspect + tests
- `argtuner inspect` shows the dependency (e.g.
  `{momentum}: … parent optimizer ∈ [sgdt]`).
- Tests: config serialization round-trip; a two-level conditional (grandchild)
  validates; inactive arg is absent from the rendered command; active arg is
  present; discrete pool skips invalid combos; `hp.*`/space omit inactive params.

---

## Phase 3 — Epic C: Multi-objective optimization

**Goal:** optimize several objectives against a Pareto frontier, not a single
scalar.

### C1. Config + protocol (`src/project/config.rs`, `src/validate.rs`)

- New serde struct `Objective { name: String, goal: Goal, primary: bool }`
  (reuses the existing `Goal` `Min`/`Max`; `primary` defaults `false`, `goal`
  defaults `Min`). TOML form (note: `goal = "min"`/`"max"` reuse `Goal`, not the
  plan's illustrative `"minimize"`):
  ```toml
  [[project.objectives]]
  name = "loss"
  goal = "min"
  primary = true

  [[project.objectives]]
  name = "latency_ms"
  goal = "min"
  ```
- `ProjectSettings` gains `#[serde(default)] objectives: Vec<Objective>`;
  `ProjectConfig` gains `objectives: Vec<Objective>` threaded through
  `from_sections` (config.rs:42). Backward-compatible: absent → empty vec.
- Validation in `validate_project_config` (validate.rs), called after
  `space.validate_specs()`:
  - objectives non-empty ⇒ names unique;
  - objectives non-empty ⇒ **exactly one `primary = true`** (any scheduler) —
    guarantees a deterministic primary index for the scalar `metric`/`score`
    fallback columns and for SHA truncation (no implicit default);
  - objectives non-empty + `Sampler::Pso` ⇒ error ("PSO is single-objective; use
    `random`") — PSO/`ControllableObjective` stay scalar/unchanged.
- No wire/protocol change (payload already carries every `metric.*` field).

### C2. Pareto engine + sampler (`src/sampler/pareto.rs` NEW, `src/sampler/random.rs`)

- Pure functions in `src/sampler/pareto.rs`:
  - `dominates(a: &[f64], b: &[f64]) -> bool` — all `<=` and at least one `<`.
  - `fast_nondominated_sort(records) -> Vec<Vec<idx>>` (Deb's fast sort).
  - `crowding_distance(front, normalized) -> Vec<f64>` — **zero-variance
    guard**: skip any objective whose span `fmax - fmin` has `abs() < 1e-12`
    (contribution 0) so a front with identical values on an objective never
    yields `NaN`/`Inf` and the subsequent sort stays total (`total_cmp`).
  - `pareto_front(records, objectives) -> front` — normalizes internally
    (`maximize → -score`), dominance on normalized vectors; raw metrics stay
    un-negated for display/persistence.
- `ParetoFront` struct: incrementally maintained non-dominated set
  (`update(trial_id, normalized, raw) -> Vec<removed>`), optional
  crowding-based cap.
- Score vector source: new
  `CommandObjective::eval_vector_with_overrides_retryable(coords, overrides,
  trial_id) -> Result<(Vec<f64>, usize), (String, usize)>` returning the
  **normalized** per-objective scores (`score[i] = if goal==Min {m} else {-m}`);
  for a single-objective run it returns `vec![primary]`. The existing scalar
  `eval_with_overrides_retryable` stays (returns the primary score) for
  PSO/`ControllableObjective` (unchanged).
- `run_scheduled` (random.rs:101-222) switches to the vector eval + Vec-based
  `record_result`; `CompletedTrial.score` becomes `Option<Vec<f64>>`
  (random.rs:20). `load_completed_trials` (random.rs:224-252) reconstructs each
  vector **in `objectives` declaration order** — it maps over the configured
  objective list and reads the exact `score.<name>` key for each (never an
  unordered iteration of `score.*` keys), falling back to `vec![FIELD_SCORE]`
  for legacy single-objective rows. `run_pareto` = the same loop maintaining a
  `ParetoFront` and printing the frontier at the end; `run_random`
  (single-objective) is unchanged in observable behavior.
- `tuner/mod.rs` dispatches: objectives empty ⇒ `run_random`/`run_pso` as today;
  objectives set ⇒ `run_pareto` (config still `type = "random"`; PSO rejected in
  C1).

### C3. Scheduler + persistence + reporting

- `TrialScheduler::record_result(token, score: Option<f64>)` →
  `record_result(token, scores: Vec<f64>)` (scheduler/mod.rs:30).
  - `FixedScheduler` (fixed.rs:120): signature only, body stays empty.
  - `SuccessiveHalvingScheduler` (successive_halving.rs:187-190): keeps its
    scalar `scores: BTreeMap<usize, f64>` and stores
    `scores.get(primary_index).copied().unwrap_or(f64::INFINITY)`; add
    `with_primary_index(usize) -> Self` builder (default 0) so
    `SchedulerBinding::build` (scheduler/mod.rs:69-91) can pass the config's
    primary index without breaking the 7-arg `new_with_seed` tests. `promote`
    (successive_halving.rs:92-119) unchanged (already sorts primary scalar).
- `src/command/objective.rs`:
  - `CommandObjective` gains `objectives: Vec<Objective>`; `new` keeps its
    signature (objectives empty ⇒ single fallback `{metric_key, goal, primary:
    true}`) plus builder `with_objectives(Vec<Objective>) -> Self` (used by
    tuner) — no churn at the ~10 existing constructor sites.
  - **Cardinality validation at ingestion** (replaces `get_metric(&metric_key)`
    at objective.rs:343): extract one value per objective via
    `payload.get_metric(&obj.name)`; any missing/non-numeric metric → trial
    rejected and marked `error` with `"missing objective metric: <name>"` —
    never a short vector, so `scores[primary_index]` can't go out of bounds.
  - Persistence (objective.rs:365-375): keep `metric.<name>` (raw, already
    carried by `payload.to_fields()`), set `metric` = primary name, `score` =
    primary signed score (backward compatible), and add `score.<name>` per
    objective (signed/normalized) so the vector is reconstructable on resume.
    Same for epoch rows (objective.rs:394-430) via the primary.
  - "new best" print (objective.rs:444-450) gated to single-objective.
- Reporting (`src/analysis/mod.rs`, `src/tuner/mod.rs:128-129`):
  - Keep `print_top_trials` (primary score) for the per-trial top-1 line
    (objective.rs:255) and single-objective end-of-run.
  - New `print_pareto_frontier(store, &[Objective])`: recomputes the front from
    stored `score.<name>` fields (fallback `score`) and prints a compact
    Pareto table (trial id + per-objective values + hparams), reusing the
    `SchedulerPlan::render` column style (plan.rs:108-173). End-of-run summary
    picks frontier (multi) vs top-trials (single).
  - Checkpointing = the front is recomputed from the persisted per-objective
    scores (no new on-disk format); `fields_json`/CSV need no schema change.
- `src/inspect.rs` `execution_summary` (inspect.rs:80-102): add an
  `objectives:` row (`{:<10}` idiom) listing name/goal/primary when configured.
  Regenerate the README inspect fixture and update
  `tests/readme_assertions.rs`.
- TUI (`src/cli/tui/mod.rs`): minimal — `build_trial_items` (1140-1151) shows
  all `metric.*` values for multi-objective trials; charts already enumerate
  `metric.*` (1402-1415) and `metric` field already selects the primary for
  coloring (`metric_for_trial` 1123-1134); details pane already lists all
  metrics.

### C4. Tests

- `src/sampler/pareto.rs` unit tests: dominance, Deb sort on a known set,
  crowding distance, normalize (maximize negation).
- `objective.rs`: missing objective metric → trial `error` "missing objective
  metric: <name>"; a 2-metric payload yields the correct normalized vector and
  persisted `score.<name>` fields.
- `successive_halving.rs`: primary-index truncation keeps `ceil(n/eta)` best by
  the primary objective.
- End-to-end: a 2-objective mock (`crates/mock-bin` new emitter) emits `loss` +
  `latency_ms`; `run_pareto` front contains exactly the true non-dominated set
  and dominated trials are excluded.

---

## Shared verification (after each phase)
1. `cargo check -p argtuner --no-default-features --lib` — the SDK build stays
   light (none of these epics touch the SDK surface; conditional `parent`/MOO
   live behind `cli`).
2. `cargo test --workspace --all-features --lib --bins --tests --examples` — green.
3. `cargo clippy --workspace --all-features --all-targets` — clean.
4. `cargo build` — binary works; `argtuner run examples/linear_regression
   --dry-run` still runs end-to-end.

## Honest scope note
These are three independent architectural features (process-group signal
handling, a search-space AST + conditional template rendering, and a
from-scratch Pareto sampler since argmin lacks NSGA-II). Each phase is a
substantial, standalone unit; the plan sequences them so each is verified before
the next builds on it.

---

## Addendum — Epic B closing gaps (agreed after implementation review)

Epic B was implemented per the plan's intent with two benign mechanism
deviations (recorded for accuracy, no rework needed):
- PTY group creation relies on portable-pty's built-in `setsid()` (child is a
  session/process-group leader) instead of `CommandBuilder::process_group(true)`,
  which does not exist in the portable-pty API; `kill(-pgid, SIGKILL)` behaves
  exactly as planned.
- Reap-sync uses a single main-thread poll loop (`wait_with_timeout`) that kills
  only while `try_wait` reports the child alive, instead of a separate watchdog
  thread + shared reaped-flag. Same invariant (never signal a recycled PGID),
  no cross-thread race.
- `trial_timeout_s` is `u64` default `0` (not `Option<u64>`); 0/absent = none.

Two gaps remain to close:

### G1. Cross-platform self-invoking subprocess tests (replaces the shell-based tests)

The `sh`/`sleep`/`echo`-based tests are not Windows-compatible (`sh`, `sleep`
don't exist on Windows). Replace them with a single self-invoking pattern: the
test binary re-executes **itself** (`std::env::current_exe()`) filtered to one
helper test via the libtest filter arg, with an env var selecting the role.

**`src/test_support.rs` additions** (module is already `cli`-gated):
- Consts: `SELF_ROLE_ENV = "ARGTUNER_SELF_ROLE"`,
  `SELF_PID_FILE_ENV = "ARGTUNER_SELF_PID_FILE"`,
  `SELF_HEARTBEAT_ENV = "ARGTUNER_SELF_HEARTBEAT"`, and private
  `SELF_TEST_FILTER = "test_support::self_invoking_helper"`.
- `pub fn self_invoking_command() -> String`: builds
  `{quoted_exe} --exact {SELF_TEST_FILTER}` with **platform-specific quoting**:
  - `#[cfg(not(windows))]`: `shell_words::quote(&exe.to_string_lossy())`
    (POSIX single-quoting).
  - `#[cfg(windows)]`: double-quoted `"{exe}"` — `split_command_windows`
    parses `"`-delimited tokens per Windows rules; POSIX single-quoting is NOT
    understood by the Windows tokenizer and would leave literal `'` in the exe
    path (`NotFound` at spawn).
  - The runner splits this with `shell_words` (Unix) / `split_command_windows`
    (Win); quoting the path handles spaces (`/Volumes/2TB Storage Vault/...`).
  - **`--exact` is mandatory:** without it libtest treats the filter as a loose
    substring and could run multiple matching tests; with `--exact` +
    `test_support::self_invoking_helper` exactly one test runs.
- `#[test] pub fn self_invoking_helper()` — role dispatcher reading
  `SELF_ROLE_ENV`:
  - `noop`: returns immediately (harness exits 0).
  - `sleepy`: sleeps 100s (timeout / cancellation target).
  - `grandchild`: writes `std::process::id()` to `SELF_PID_FILE_ENV`, then loops
    writing a monotonically increasing counter to `SELF_HEARTBEAT_ENV` every
    100ms (the liveness signal below).
  - `child`: spawns `current_exe()` with args `["--exact", SELF_TEST_FILTER]`
    and env `SELF_ROLE_ENV=grandchild` + the propagated `SELF_PID_FILE_ENV` +
    `SELF_HEARTBEAT_ENV`, then `wait()`s.
  - unset: no-op pass (normal `cargo test` run of the helper).
  - **Correctness linchpin:** every self-spawn passes `--exact
    {SELF_TEST_FILTER}`, so the subprocess harness runs exactly this one test —
    never the whole suite (prevents recursive test explosion). A mismatch fails
    loudly (child exits 0 → driver's `timed_out`/exit-code assertions fail), not
    silently.
- **Liveness = heartbeat, not `kill(pid, 0)`.** On Unix `kill(pid, 0)` returns
  success for **zombies** (terminated but unreaped), so an `is_process_alive`
  probe reports a zombie as alive and flakes on CI. Instead a running grandchild
  advances its heartbeat file; a dead or zombie grandchild cannot. Add
  `pub fn assert_no_longer_running(pid: u32, heartbeat_file: &Path,
  grace: Duration) -> ()`: reads the heartbeat size, sleeps `grace` (≥ 2
  heartbeat intervals), re-reads; asserts unchanged (with a message naming the
  pid). Cross-platform, no `/proc`, no sysctl, no Windows handle API.
- No new dependencies and **no `Cargo.toml` change** (the earlier
  `Win32_System_Threading` idea is dropped — no Windows handle API is used).

**`src/command/subprocess/runner.rs` tests — rewrite all four subprocess tests**
(currently `echo` / `sleep 30` / `sh -c`):
- `run_piped_completes_without_options`: role `noop`; assert exit 0,
  `!timed_out`.
- `run_times_out_and_kills_process_group`: role `sleepy`, `timeout` = 2s; assert
  `timed_out`, exit != 0, elapsed < 15s.
- `run_stop_flag_kills_process_group_without_timeout`: role `sleepy`, stop flag
  flips after 300ms; assert `!timed_out`, exit != 0.
- `run_timeout_kills_grandchild_processes`: role `child` +
  `SELF_PID_FILE_ENV`/`SELF_HEARTBEAT_ENV` = two `tempfile::tempdir()` paths,
  `timeout` = 2s; assert `timed_out`; parse the grandchild pid (defensive
  diagnostic if the pid file is missing — the grandchild writes it on boot,
  well before the 2s deadline); then
  `assert_no_longer_running(pid, &heartbeat, 600ms)` — proves the group kill
  stopped the whole tree.
- All pass the role/heartbeat paths through the runner's `envs: BTreeMap`
  (never the process env) → no cross-test races.

**`src/command/objective.rs` — convert
`objective_times_out_long_command_and_marks_error`** (currently
`CommandTemplate::new("sleep 30")`, Unix-only):
- Template = `self_invoking_command()`. Inject the role without the process env:
  `TrialOverrides { env: {SELF_ROLE_ENV: "sleepy"}, ..Default::default() }` via
  `eval_with_overrides(&[], &overrides)` — `render_trial_command_with_overrides`
  already copies `overrides.env` into `rendered.env` (src/trial/mod.rs:66), which
  the runner sets on the child. Assertions unchanged (`timed out` error + trial
  marked `error`).

**Deletion:** remove the current shell-based `run_timeout_kills_grandchild_processes`.

### G2. Full verification (plan's shared checklist, previously skipped)
Run and confirm green:
1. `cargo check -p argtuner --no-default-features --lib` (SDK stays light).
2. `cargo test --workspace --all-features --lib --bins --tests --examples` —
   now includes the cross-platform subprocess tests (the runner + objective
   timeout tests no longer depend on `sh`/`sleep`, so Windows CI runs them too).
3. `cargo clippy --workspace --all-features --all-targets` — clean.
4. `cargo build`; `argtuner run examples/linear_regression --dry-run` still runs
   end-to-end.

No changes to the runner supervision logic itself — these are test + verification
only.

---

## Addendum — Epic C follow-up (agreed after implementation review)

Epic C matched the plan's intent with three benign deviations (no standalone
`pareto_front` wrapper — rank-0 of `fast_nondominated_sort` used at call sites;
`with_primary_index` is a `&mut self` setter not a `-> Self` builder; README
inspect fixture unchanged because single-objective output is byte-identical).
Two scope reductions from review remain to close:

### C-F1. Crowding-distance-managed `ParetoFront` (`src/sampler/pareto.rs`)

- `ParetoFront::with_capacity(usize)`; `new()` defaults to a bounded capacity
  (e.g. 200) so `crowding_distance` is exercised in production (it is currently
  only unit-tested — dead code in the sampler).
- In `update()`: after inserting a non-dominated point, if
  `entries.len() > capacity`, compute `crowding_distance` over all entries and
  evict the **non-boundary** (finite-distance) entry with the smallest distance;
  if every entry is a boundary (all `INFINITY`), evict the oldest. The evicted
  id joins the returned `Vec<usize>` (run_pareto's progress line already prints
  front size).
- Bounds the frontier to `O(capacity)` memory; dominance checks stay
  `O(M·N²)` in the frontier size N ≤ capacity.
- Tests: `front_update_respects_capacity` (cap 3; insert four non-dominated
  points where the interior has the smallest crowding distance; assert
  `len() == 3` and the interior point was evicted, boundaries kept); existing
  `front_update_*` tests unchanged.

### C-F2. Watch TUI Pareto-frontier view (`src/cli/tui/mod.rs`)

- New `AppComponent::Frontier(ScrollViewComponent<TextRendererComponent>)`
  (mirrors `ProjectInfo` at mod.rs:336), opened in `run()` (mod.rs:59-76) with
  title `"Pareto Frontier"`, non-closable, stored in `AppState` (window key +
  `frontier_sv()` accessor).
- New `fn frontier_lines(trials: &[TrialRow]) -> Vec<Line<'static>>`:
  - reconstruct each ok trial's signed vector from `score.<name>` fields
    (fallback `score`);
  - `fast_nondominated_sort` → rank-0 ids;
  - lines: header `Pareto frontier (k of n trials)`, then per non-dominated
    trial `Trial {id}  {objective}={value:.4} …` plus its hparams, reusing the
    `SchedulerPlan::render` column style.
  - single-objective runs (no `score.<name>` fields) fall back to the top trial
    by `score` with a "single-objective" note.
- Refresh `set_text` in `refresh_trials` (mod.rs ~490-511) each pass so the
  panel tracks new completions live.
- `build_trial_items` (mod.rs ~1151) marks non-dominated trials (computed via
  the same rank-0 set) with an `nd`/`frontier` tag so operators see them at a
  glance in the Trials list; the existing per-metric charts already plot every
  `metric.*` axis.

Verification for both: `cargo test --workspace --all-features`, clippy clean,
and a manual `argtuner watch <multi-objective-project>` confirming the Frontier
panel lists exactly the non-dominated trials while trials are still running.

---

## Addendum — Epic C runnable demo (user-requested example)

A self-contained example under `examples/pareto_demo/` so the feature can be
seen in the end-of-run summary and the Watch TUI. The user's sketch is
translated to the **real** config schema and talkback protocol:

### Files
- `examples/pareto_demo/argtuner.toml` — corrected schema (the sketch's
  `name = "pareto_demo"`, `range = [...]`, lowercase `type` are not in the real
  schema and would be rejected by `deny_unknown_fields`; the run command lives
  in `template`, not a `--command` flag — `argtuner run` takes a project dir):
  ```toml
  template = "python3 train.py --learning-rate {learning_rate} --batch-size {batch_size} --checkpoint-dir {trial_dir}"

  [project]
  metric_key = "loss"
  goal = "min"

  [[project.objectives]]
  name = "loss"
  goal = "min"
  primary = true

  [[project.objectives]]
  name = "latency_ms"
  goal = "min"

  [sampler]
  type = "random"

  [scheduler]
  type = "fixed"
  n_trials = 6

  [space]
  [[space.params]]
  type = "Float"
  name = "learning_rate"
  min = 0.001
  max = 0.1
  log = true

  [[space.params]]
  type = "Choice"
  name = "batch_size"
  values = ["16", "32", "64"]
  ```
  (`--checkpoint-dir {trial_dir}` is mandatory validation; cwd is the process
  dir, so the README/demo runs `cd examples/pareto_demo && argtuner run .`.)
- `examples/pareto_demo/train.py` — accepts `--learning-rate`, `--batch-size`,
  `--checkpoint-dir`; simulates the trade-off (`batch_size=16` → lower loss,
  higher latency); emits the **`::ARGTUNER::` protocol** (a plain `print(json)`
  is ignored by the parser) — e.g.
  `::ARGTUNER::{"type":"event","name":"model.epoch_end","fields":{"loss":"0.067812","latency_ms":"74.21"}}`
  with a short `time.sleep(0.5)` so the TUI frontier panel updates live.

### Runs (after C-F1/C-F2 are built)
- `cd examples/pareto_demo && argtuner run .` → end-of-run `Pareto frontier (k
  of 6 trials):` table of non-dominated trials + hparams.
- `argtuner watch --project examples/pareto_demo` (separate terminal, while the
  run is active) → live `Pareto Frontier` panel enumerating non-dominated
  trials and `[nd]` tags in the Trials list (per C-F2).
- `argtuner inspect .` shows `objectives: loss(minimize) (primary),
  latency_ms(minimize)`.

### Verification
Run the demo once and capture the end-of-run frontier block; assert the demo's
`argtuner inspect` output includes the objectives line; full workspace tests +
clippy stay green.

---

## Addendum — pareto demo: real metric curves (user-observed)

**Problem:** the Watch TUI charts showed a single dot per trial. The charts
(`render_metric_charts` → `metric_series_for_key`, src/cli/tui/mod.rs:1542-1614)
build one curve from all **epoch rows**, and the demo emitted exactly one
`model.epoch_end` per trial — so each trial contributed one point.

**Fix — `examples/pareto_demo/main.rs`:** simulate a short training run per
trial. Replace the single `emit_metrics!` with a loop of ~10
`argtuner::emit_epoch_end(&fields)` calls where `loss` converges over epochs
from a worse start toward the trade-off value and `latency_ms` stays flat, with
a ~60ms sleep between epochs so the curve builds live in the TUI. No `epoch`
field is emitted (the chart's aggregate x-axis is the epoch-row index, which
stays monotonic across trials).

Result: each trial renders a visible ~10-point segment, and the end-of-run
frontier is unchanged (the final epoch's values still encode the trade-off).

### Verification
Re-run `cd examples/pareto_demo && argtuner run .` (end-of-run frontier
unchanged); confirm `cargo build --examples` and the full workspace test suite
stay green.

---

## Addendum — Watch TUI: adaptive frontier panel (user-requested)

The user found "Pareto Frontier" confusing in single-objective runs, wants the
panel to show the end-of-run summary live (both modes), and wants an explicit
"run in progress" indicator so live values aren't mistaken for final scoring.

### Changes

- **`src/cli/tui/mod.rs`**:
  - **Mode detection** helper `fn is_multi_objective(trials: &[TrialRow]) ->
    bool` — true when any trial has a `score.<name>` field (single-objective
    runs persist only `score`).
  - **Dynamic window title** in the `refresh` title block (~mod.rs:530): set
    `frontier_key` title to `"Pareto Frontier"` (multi) or `"Best Trials"`
    (single), updated each pass.
  - **`frontier_lines(trials, in_progress)`** rewrite:
    - Line 1 status: `Run in progress — results are live` when `in_progress`,
      else `Run complete — final results`.
    - empty → `(no completed trials yet)`.
    - **multi** → existing `Pareto frontier (k of n trials):` non-dominated
      table (kept).
    - **single** → mirror the end-of-run `print_top_trials` table live:
      `Best trials (top N of n):`, sorted by `score` ascending, each trial with
      `score=…` + raw `metric.<key>` value + hparams.
  - **`in_progress` = last-activity age + hysteresis** (NOT socket presence —
    an IPC handle staying open after a run finishes would report "in progress"
    forever). AppState gains `last_activity: Option<Instant>`, refreshed when
    the step subscriber yields data **or** any loaded trial/epoch/step row has
    status `"running"`. `run_in_progress() = last_activity.is_some_and(|t|
    t.elapsed() < IN_PROGRESS_HOLD)` with `IN_PROGRESS_HOLD = 10s` — the hold is
    the hysteresis: it covers inter-trial scheduling gaps (no flicker) and
    flips to "complete" only after 10s of silence after the run ends. Covers
    both watch-start orderings (before/after the run). No `store.rs` change.
  - **`[nd]` tags** (`build_trial_items`): only apply when
    `is_multi_objective(trials)` — currently rank-0 of 1-D vectors tags every
    single-objective trial as non-dominated, which is wrong.

- **`examples/pareto_demo/main.rs`**: emit `epoch` explicitly in each
  `model.epoch_end` payload (`fields.insert("epoch", epoch.to_string())`) so
  the charts' x-axis uses real epoch numbers (overlaid per-trial training
  curves) instead of implicit row indices.

- **Tests** (`frontier_tests`, `src/cli/tui/mod.rs`): single-objective rows
  (only `score`, no `score.<name>`) → header contains `Best trials`, best trial
  first, and **no** `[nd]` tags; `in_progress = true` → header contains
  `in progress`, `false` → `complete`; existing multi-objective assertions
  unchanged.

### Verification
`cargo test --workspace --all-features`, clippy clean, and a manual
`argtuner watch` on the pareto demo (multi → "Pareto Frontier") and on
`examples/linear_regression` (single → "Best Trials") to confirm titles, live
top-trials table, and the in-progress line.
  