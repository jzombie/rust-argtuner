# Doc review: fix README nit + add auto-derive note

Review verdict: CHANGELOG, code, and readmes are accurate. Two small doc fixes.

## 1. README.md:453 — trait/struct shadow wording
`(IpcChannel, TunerParams)` → `(IpcChannel, TrainParams)` (the doc struct name;
`TunerParams` is the SDK trait, re-introduces the shadowing we fixed).

## 2. README "Declaring parameters" — auto-derive note (gap, optional)
Add a sentence: `#[tuner_params]` auto-derives `Debug`, `Clone`, and
`serde::Serialize` (resolved through `argtuner_sdk`), so don't add your own
`#[derive(...)]` on the struct.

## No other changes
- CHANGELOG: released section = verbatim original (accurate); Unreleased entries verified
  against derive code — no edits.
- sdk/derive/common/interactive_probe READMEs, AGENTS.md, code doc comments: accurate.

## Verification
1. `git grep -nE "\(IpcChannel, TunerParams\)|struct TunerParams|init::<TunerParams>"` → zero.
2. `cargo test -p argtuner --doc` (README doctests still compile).
